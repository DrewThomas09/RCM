version: "3.8"

services:
  rcm-mc:
    build: .
    volumes:
      - ./configs:/app/configs:ro
      - ./outputs:/app/outputs
    command: >
      --actual configs/actual.yaml
      --benchmark configs/benchmark.yaml
      --outdir outputs
      --n-sims 30000
