"""Phase 4 — Value Creation Model.

This phase reuses existing infrastructure: per-root-cause recoverable
EBITDA feeds ``rcm_mc.pe.rcm_ebitda_bridge`` (and v2 when wired), Monte
Carlo on payer behavior reuses ``rcm_mc.mc.ebitda_mc``, and QoE-ready
output reuses ``rcm_mc.exports.packet_renderer``. Phase 4 is wiring,
not new math.

Empty in this session. Implementation ships in a follow-up.
"""
from __future__ import annotations

__all__: list[str] = []
