# Pins Tuva at the latest stable release per
# https://github.com/tuva-health/tuva/releases (v0.17.2, 2026-04-02,
# non-prerelease — verified via gh api before writing this file).
# Upgrade deliberately — a minor-version bump can rename input-layer
# columns. Keep in sync with the versions referenced in
# rcm_mc_diligence/SESSION_LOG.md.
packages:
  - package: tuva-health/the_tuva_project
    version: 0.17.2
